export enum AccessType {
  public = 'public',
  team = 'team',
  private = 'private',
}

export enum AccessRequestStatus {
  noAccess = 'noAccess',
  pending = 'pending',
  approved = 'approved',
  rejected = 'rejected',
}
