export enum Role {
  admin = 'admin',
  staff = 'staff',
  vendor = 'vendor',
  owner = 'owner',
}

export enum Status {
  active = 'Active',
  inactive = 'Inactive',
}

export enum UserCreatedEventRoute {
  FORM = 'FORM',
  SOCIAL = 'SOCIAL',
  OKTA = 'OKTA',
}
